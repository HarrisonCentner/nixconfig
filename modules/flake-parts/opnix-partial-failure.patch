diff -ruN a/internal/onepass/client.go b/internal/onepass/client.go
--- a/internal/onepass/client.go	2026-08-06 14:22:59.102618333 +0000
+++ b/internal/onepass/client.go	2026-08-06 14:23:25.106922741 +0000
@@ -147,7 +147,7 @@
 	}
 
 	if len(failures) > 0 {
-		return nil, &errors.ProviderResolutionError{Failures: failures}
+		return resolved, &errors.ProviderResolutionError{Failures: failures}
 	}
 
 	return resolved, nil
diff -ruN a/internal/secrets/processor.go b/internal/secrets/processor.go
--- a/internal/secrets/processor.go	2026-08-06 14:22:59.102696054 +0000
+++ b/internal/secrets/processor.go	2026-08-06 14:23:17.633096058 +0000
@@ -70,10 +70,10 @@
 	}
 
 	references := uniqueReferences(cfg.Secrets)
-	resolvedSecrets, err := p.client.ResolveSecrets(references)
-	if err != nil {
+	resolvedSecrets, resolveErr := p.client.ResolveSecrets(references)
+	if resolveErr != nil && len(resolvedSecrets) == 0 {
 		return nil, errors.WrapWithSuggestions(
-			err,
+			resolveErr,
 			"Resolving 1Password references",
 			"secret processing",
 			[]string{
@@ -88,11 +88,8 @@
 		secretName := fmt.Sprintf("secret[%d]:%s", i, secret.Path)
 		value, ok := resolvedSecrets[secret.Reference]
 		if !ok {
-			return nil, errors.OnePasswordError(
-				fmt.Sprintf("Resolving secret %s", secretName),
-				fmt.Sprintf("Failed to resolve 1Password reference: %s", secret.Reference),
-				nil,
-			)
+			fmt.Fprintf(os.Stderr, "WARNING: Skipping %s: unresolved 1Password reference %s\n", secretName, secret.Reference)
+			continue
 		}
 
 		outputPath, err := p.processSecret(secret, secretName, value)
@@ -113,6 +110,19 @@
 		result.ProcessedCount++
 	}
 
+	if resolveErr != nil {
+		return nil, errors.WrapWithSuggestions(
+			resolveErr,
+			fmt.Sprintf("Resolving 1Password references (wrote %d resolvable secrets)", result.ProcessedCount),
+			"secret processing",
+			[]string{
+				"Check the failed 1Password references listed above",
+				"Create any missing vaults, items, or fields before restarting opnix-secrets.service",
+				"If rate-limited, wait for the 1Password reset window before retrying",
+			},
+		)
+	}
+
 	return result, nil
 }
 
